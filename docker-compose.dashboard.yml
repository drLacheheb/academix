services:
  # Standalone Modern Rust + React Database Dashboard (DockAdmin)
  academix-db-dashboard:
    image: demlabz/dockadmin:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
